{
  "name": "freeshop-proxy",
  "version": "0.1.0",
  "description": "Cloudflare Worker that proxies the Switch client's catalog/icon/download reads to the GitHub data repo, so raw.githubusercontent.com/jhon1466/... never shows up in sources.json or on the wire",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
