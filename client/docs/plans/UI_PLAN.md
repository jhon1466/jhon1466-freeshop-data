# pipensx — план улучшения UI/UX

Цель: опыт максимально простой, интерфейс в духе системного UI Switch и оригинального eShop.
Аудит от 2026-07-02. Исходное состояние: весь UI в `src/main_switch.cpp` (~4150 строк),
46 захардкоженных `nvgRGB`, темы Borealis не используются, каталог из 6901 записи —
плоский список, все действия на скрытых кнопках (X/Y/LB/RB), тач-жестов и звуков нет,
i18n нет.

Задачи разбиты по сложности на три пула — **Fable** (сложные), **Opus** (средние),
**Sonnet** (простые). Внутри пула порядок = рекомендуемый порядок выполнения.
Зависимости указаны явно; задачи без зависимостей можно вести параллельно.

Палитра (фиксируем как токены): акцент `#00C3E3` (Joy-Con Neon Blue), ошибка `#FF4554`
(Neon Red), успех `#60DC82`, поверхности/текст — из тем Borealis light/dark.

---

## Fable — архитектура и новые компоненты

### F1. Распил `main_switch.cpp` на модули ✅ (выполнено)
Разнести UI по `src/ui/{common,catalog,detail,downloads,installed,settings}/*.{cpp,hpp}`:
вью, ячейки, дата-сорсы, активности. Поведение не менять (чистый move + include-гигиена).
Блокирует все остальные задачи — делать первой.
**Готово, когда:** `main_switch.cpp` < 300 строк (main + wiring), Switch-сборка `pipensx.nro`
собирается, PC-тесты зелёные.

### F2. Грид обложек + полки eShop в каталоге ✅ (выполнено)
Вместо списка 96px-строк — грид карточек ~180×180 (обложка + чистое имя из
`GameMetadataService`; сырой заголовок RuTracker уходит в карточку игры). Реализация —
«ряд из N ячеек» в `RecyclerFrame` (паттерн wiliwili), фокус-навигация по сетке,
переиспользование ячеек, `AsyncRgbaImage` + существующий дисковый кэш. Над гридом две
горизонтальные полки: «Новое» (`published_at`), «Популярное» (`peer_count`) — данные
уже в `catalog.json`.
Зависит от F1.
**Готово, когда:** плавный скролл на консоли по всем 6901 записям, фокус не теряется
при `reloadData`, полки листаются стиком и тачем.

### F3. Карточка игры — двухколоночный layout eShop ✅ (выполнено)
Слева: обложка, под ней Install-кнопка на всю ширину колонки, размер, статус.
Справа: название, факты, галерея скриншотов, описание. Переработка `GameDetailActivity`
(сейчас вертикальная простыня с баннером 220px).
Зависит от F1; таблицу фактов (S4) и «Show more» (S5) закладывать сразу в новый layout.
**Готово, когда:** экран визуально соответствует референсу eShop в docked и handheld,
обе темы.
**Выполнено 2026-07-04:** `GameDetailActivity` перестроена: корневой ROW-бокс; слева
фиксированная колонка 320px — обложка (iconUrl, fallback bannerUrl), Install и Options
на всю ширину колонки, размер, статус; справа собственный `ScrollingFrame` — название
(шрифт 32), таблица фактов (S4), галерея скриншотов, описание с «Show more» (S5),
health/failure-предупреждение и сырой заголовок релиза (переехал из списка по F2).
Все цвета/шрифты — токены O1, `nvgRGB` вне theme.hpp по-прежнему 0. Switch-сборка
`pipensx.nro` и PC-тесты зелёные; визуальная сверка с референсом eShop
(docked/handheld, обе темы) — при первом прогоне на железе, golden-CI F4 закроет
регрессионно.

### F4. Golden-скриншот CI ✅ (выполнено)
PC-сборка (GLFW) рендерит каждый экран в light и dark, снимает скриншоты offscreen,
сравнивает с эталонами в репо; diff — артефакт CI. Инфраструктура: headless-рендер,
детерминированные данные (фикстурный каталог), допуск по пикселям.
Зависит от F1 (экраны должны конструироваться изолированно).
**Готово, когда:** job падает на визуальную регрессию, эталоны обновляются одной командой.
**Выполнено 2026-07-04:** PC-шим libnx (`src/platform/pc/`) + desktop-сборка
`-DPIPENSX_GOLDEN=ON` → `build-golden/golden_runner`: поднимает реальные сервисы на
фикстурах `tests/fixtures/golden/` (3 записи каталога, 2 метаданных; сеть выключена,
образы — плейсхолдеры), рендерит 6 экранов
(catalog/detail/downloads/installed/settings/about) × light/dark в 1280x720 PNG.
Эталоны в `tests/golden/` (12 шт.); `scripts/golden.sh check|update` — сверка и
однокомандный ре-бейзлайн. Допуск: ImageMagick AE, fuzz 5%, бюджет 25000 px —
пульс подсветки фокуса у borealis идёт от wall-clock и даёт ~9k px шума между
одинаковыми прогонами (llvmpipe); `LIBGL_ALWAYS_SOFTWARE=1` уравнивает растеризатор
локально и в CI. Workflow `.github/workflows/golden.yml`: xvfb-run на ubuntu, при
падении выгружает текущие рендеры и diff-маски артефактом `golden-current`.

### F5. Полки каталога 2.0: «Популярное», дедупликация, визуал ✅ (выполнено)
Добавлено 2026-07-04 по итогам прогона. Три проблемы:
1. Полка «Популярное» не видна. Код из F2 есть (`catalog_view.hpp:563–595`),
   `peer_count` парсится (`catalog_service.cpp:351`), но записи с `peerCount == 0`
   отфильтровываются (`catalog_view.hpp:592`) — если источник каталога peer_count
   не отдаёт, полка молча исчезает. Диагностировать данные; при пустом peer_count —
   fallback-ранжирование (свежесть + размер/число файлов) вместо скрытия, полку
   не прятать молча.
2. Игры дублируются: «Новое» и «Популярное» считаются независимо, пересечение
   не вычитается, плюс те же игры сразу в гриде. Дедупликация: игра максимум на
   одной полке (приоритет Популярное > Новое), переиздания одного тайтла внутри
   полки схлопывать по titleId; в гриде — всегда.
3. Полки визуально однообразны. Hero/featured-баннер сверху, доп. полки
   («Обновлённые», жанровые подборки из `GameMetadataService`), заголовки секций
   с «See all →» (переход в грид с соответствующей сортировкой), фокус-скейл
   карточки.
Зависит от F2, O1; согласовать с O10 (бейджи) и O11 (навигация).
**Готово, когда:** «Популярное» видна на реальном каталоге, ни одна игра не
встречается на двух полках одновременно, golden-эталоны каталога обновлены.

### F6. In-memory кэш обложек — мгновенный повторный вход в каталог ✅ (выполнено)
Добавлено 2026-07-04. Сейчас `AsyncRgbaImage` при каждом создании ячейки заново
читает дисковый кэш и декодирует PNG/JPEG — после выхода из каталога и повторного
входа иконки «прогружаются» с нуля.
Объём: общий LRU-кэш в памяти (декодированный RGBA или живые NVG-текстуры) с
бюджетом ~64–128 MB поверх существующего дискового кэша; попадание в кэш —
синхронная подстановка в первый кадр, без мигания плейсхолдера; префетч
соседних строк грида и полок; инвалидация при обновлении каталога; аккуратная
жизнь текстур относительно NVG-контекста и потоков декодера.
Зависит от F2.
**Готово, когда:** повторный вход в каталог показывает уже виденные обложки в
первый же кадр, скролл по всем 6901 записям без роста памяти сверх бюджета
(замер на консоли).
**Выполнено 2026-07-05:** кэш живёт в `GameMetadataService`: существующий LRU
декодированного RGBA (`imageCache_`) поднят с 24 до 96 MB (середина окна
64–128 MB, бюджет держит LRU-вытеснение в `cacheImageLocked`). Синхронный
пробник `cachedImage()`: `loadImageInto` при хите кладёт NVG-текстуру в тот же
кадр (`AsyncRgbaImage::setRgbaNow`, UI-поток), минуя worker-очередь — без
мигания плейсхолдера. Префетч: `prefetchImage()` (декод без колбэка, коалесится
с обычными запросами) греет соседние грид-строки из
`CatalogDataSource::cellForRow` (`prefetchGridRow(row±1)`); полки/hero биндят
все свои карточки сразу и греются сами. Инвалидация: `dropMemoryImageCache()`
при успешном `refreshCatalog()` (дисковый кэш остаётся; очистка из Settings
чистит оба). Жизнь текстур не изменилась: кэш хранит immutable RGBA
(`shared_ptr<const DecodedImage>` из потоков декодера), NVG-текстурами
по-прежнему владеют сами вью. Тест `testImageMemoryCache` (PC): холодный
промах, префетч-прогрев, коалесс запроса на кэшированную копию, дроп.
Switch-сборка `pipensx.nro`, PC-тесты и golden зелёные; мгновенный повторный
вход и замер памяти по бюджету — при первом прогоне на железе.

---

## Opus — компонентные фичи в существующих паттернах

### O1. Дизайн-токены + темы light/dark ✅ (выполнено)
Один `ui/theme.hpp`: акцент, три уровня текста, поверхности, статусные цвета, радиусы
6/8/12, шкала шрифтов 32/25/21/17/15, шаг отступов 8px. Заменить все 46 `nvgRGB`.
Подключить `brls::Theme`, следовать теме консоли (сейчас светлая тема сломана —
тёмные плашки останутся на белом).
Зависит от F1 (иначе конфликты в монолите). Вторая по порядку после F1.
**Готово, когда:** `grep -c nvgRGB src/ui | вне theme = 0`, оба варианта темы проверены
на каждом экране.
**Выполнено 2026-07-04:** `src/ui/theme.hpp` — токены `pipensx/*` регистрируются в обе
`brls::Theme` (light/dark) через `registerColors()` после `Application::init()`;
аксессоры цветов + константы радиусов (6/8/12), шкалы шрифтов (32/25/21/17/15) и шага
отступов (8). Заменён 51 `nvgRGB` (46 из аудита + 5 локальных статусных из S1, теперь
на токенах); вне theme.hpp = 0. Светлой теме даны свои поверхности и уровни текста —
тёмные плашки на белом устранены; вариант темы берётся у консоли (SwitchPlatform).
Switch-сборка и PC-тесты зелёные; визуальный прогон обеих тем на консоли — при первом
запуске на железе (golden-CI F4 закроет регрессионно).

### O2. Header каталога: поиск, чипы, счётчик ✅ (выполнено)
Постоянная верхняя панель: тапабельное поле поиска (сейчас только кнопка X), чипы
сортировки Latest/A–Z/Size (сейчас слепой цикл по Y) и фильтра All/Games (сейчас
закопан в Settings), счётчик результатов. Хоткеи X/Y сохранить. Тач обязателен.
Зависит от F1, O1; согласовать с F2.
**Выполнено 2026-07-04:** постоянный header-ряд в `CatalogView`: тапабельное
поле поиска (показывает текущий запрос, тап открывает IME; чип Clear при
непустом запросе), чипы Latest/A-Z/Size (`setSort`) и All/Games (`setFilter`,
пишет в `AppSettings` — экран Settings синхронизируется через generation),
счётчик результатов справа. Активный чип — `BUTTONSTYLE_PRIMARY`. Хоткеи X/Y
сохранены (Y-цикл теперь без тоста — состояние видно на чипах). Старая
строка статуса скрыта вне батч-режима (счётчик и фильтр переехали в header),
батч-сводка на месте. Всё на токенах O1. Switch-сборка и PC-тесты зелёные;
тач-прогон на консоли — при первом запуске на железе.

### O3. i18n en + ru
Механизм i18n форка Borealis (wiliwili), вынести все строки, перевести на русский —
основная аудитория. Убрать принудительный `LOCALE_EN_US`, следовать языку консоли
с fallback en.
Зависит от F1.

### O4. Батч-режим: чекбоксы и sticky-футер
Чекбокс-оверлей на карточке вместо ASCII `[x]`/`[ ]`/`[-]`; снизу закреплённая панель
«N выбрано · 12.3 GB · SD free 40 GB · [Prepare]» — логика `refreshBatchStatus` готова,
меняется подача.
Зависит от F2.

### O5. Кнопка-статус на карточке игры ✅ (выполнено)
Install → прогресс на кнопке («Downloading 42%») → «Installed» (как eShop). Поллинг
500 мс уже есть (`refreshButtons`); сейчас кнопка гаснет, текст живёт сбоку.
Зависит от F3.
**Выполнено 2026-07-04:** primary-кнопка стала статус-поверхностью. Новый
`InstallButton` (сабкласс `brls::Button`) во время загрузки/установки остаётся
живой (ENABLED, вместо серого DISABLED) и рисует eShop-заливку — затемняет
`theme::panel()` ещё не выполненный «хвост» пилюли (`draw` поверх фона, под
лейблом; `getCornerRadius`). Доля заливки — `progressForButton`: install-фазы по
`installProgressOf`, остальное по `progressOf`, Queued=0, Completed/Installed=1
(полная). Лейбл по-прежнему из `installButtonLabel` («Downloading N%» /
«Installing N%» / «Installed 100%»). `refreshButtons` больше не гасит кнопку;
Paused/Error → «Resume» без заливки. Тап по живой кнопке открывает
`DetailsActivity` (статус-контрол). Idle-вид («Install») не изменён — golden
detail-эталоны зелёные. Всё на токенах O1. Switch-сборка `pipensx.nro`, PC-тесты
и golden зелёные; тач/фокус-прогон на консоли — при первом запуске на железе.

### O6. Полноэкранный просмотр скриншотов ✅ (выполнено)
По A на скриншоте — fullscreen-пейджер (листание L/R и свайпом, B — назад). Сейчас
скриншоты фокусируются, но действия нет.
Зависит от F3.
**Выполнено 2026-07-04:** новый `ScreenshotViewerActivity`
(`src/ui/detail/screenshot_viewer.hpp`) — один скриншот на весь экран
(`AsyncRgbaImage`, FIT, grow), под ним счётчик «i / N», в заголовке имя игры.
Листание: LB/RB (хинты Previous/Next, repeat), D-pad ←/→ (скрытые) и
горизонтальный свайп (`PanGestureRecognizer`, порог 40 px, решение по финальной
дельте на END), индекс с закольцовкой. B — назад (`AppletFrame` регистрирует
сам). Каждый скриншот в галерее карточки игры получил `registerClickAction`,
открывающий пейджер на своём индексе (весь список URL + имя игры прокинуты). Всё
на токенах O1. Switch-сборка `pipensx.nro`, PC-тесты и golden зелёные (idle-вид
detail не изменился); тач/свайп-прогон на консоли — при первом запуске на железе.

### O7. Downloads: контекстное меню по A ✅ (выполнено)
Меню на строке: Details / Pause / Resume / Verify / Remove. Сейчас Y — три действия
одной кнопкой вслепую, X — Remove.
Зависит от F1, O1.
**Выполнено 2026-07-04:** A по строке загрузки открывает `brls::Dropdown`
(заголовок — имя задачи) с контекстным меню вместо прямого перехода в Details.
`MainView::openRowMenu` снимает снапшот задачи и собирает пункты по статусу:
Details всегда; Pause — если активна (Queued/Checking/Downloading/Installing/
Committing/Verifying); Resume — если Paused/Error; Verify — если Completed
(совпадает с `DownloadManager::verify`); Remove всегда (диалог Keep/Delete/
Cancel через `openRemoveDialog`). Слепые пункты стали подписанными и
контекстными. Dropdown снимает себя сразу после колбэка, поэтому действие
отложено на кадр через `brls::sync` (иначе `pushActivity` для Details/Remove
попал бы под pop меню). Мутации (pause/resume/verify/remove) дёргают
`startRefreshing(true)` для мгновенного обновления списка. Всё на токенах O1.
Switch-сборка `pipensx.nro`, PC-тесты и golden зелёные (статичный вид Downloads
не изменился); прогон меню на консоли — при первом запуске на железе.

### O8. DetailsActivity секциями ✅ (выполнено)
Карточки: «Прогресс» (крупный бар + ETA), «Скорость» (график оставить), «Сеть»
(peers/DHT/pieces); экранные кнопки Pause/Verify/Remove вместо хоткеев.
Зависит от F1, O1, S1 (ETA).
**Выполнено 2026-07-04:** `DetailsActivity` перестроена в скролл-колонку карточек
на токенах O1: заголовок Status (цвет — статусный токен) + ряд экранных кнопок
Pause/Resume · Verify · Remove (стили `BUTTONSTYLE_PRIMARY`/`DEFAULT`,
состояние ENABLED/DISABLED по статусу задачи, обновляется поллингом 500 мс),
карточка «Progress» (крупный `ProgressBar` 14px + процент/байты + ETA из
`formatEta` S1), «Speed» (текст скоростей + `SpeedGraphView`), «Network»
(peers/DHT/pieces). Слепые хоткеи X/Y убраны — все действия на видимых кнопках,
B (назад) остаётся от `AppletFrame`; логика pause/resume/verify/remove и
stream-install (пакеты, EMA-скорость установки) сохранена. Switch-сборка
`pipensx.nro` и PC/golden зелёные; тач/фокус-прогон на консоли — при первом
запуске на железе.

### O9. Options → выбор режима установки ✅ (выполнено)
Внятный диалог: «Download to SD» / «Stream install» с пояснениями, выбор файлов,
предупреждение о месте (`InstallSpaceEstimate` уже считается).
Зависит от F3.
**Выполнено 2026-07-04:** кнопка Options на карточке игры после резолва магнета
показывает `brls::Dialog` с выбором режима — «Stream install» (ставит игру на
консоль по мере скачивания, временная загрузка чистится) и «Download to SD»
(сохраняет файлы на SD без установки), с пояснениями к каждому. В шапке диалога —
оценка размера релиза и число файлов (`estimateInstallSpace`), «SD free» и
предупреждение о нехватке места (`assessInstallSpace` → «Need … more»), пометка
«NSZ may expand» для сжатых пакетов. Выбранный режим прокидывается как `preferred`
в существующий `TorrentSelectionActivity` (выбор файлов + жёсткая проверка места
там же). Диалог `setCancelable(false)` — B не роняет темп-торрент; Cancel его
удаляет. Всё на токенах O1. Switch-сборка `pipensx.nro` и PC/golden зелёные;
тач-прогон на консоли — при первом запуске на железе.

### O10. Бейджи состояния на обложках
Цветная угловая плашка Queued/Downloading/Installed + точка здоровья раздачи
(fresh/unknown/dead) вместо текста справа. Цвета — только токены O1.
Зависит от F2, O1.

### O11. Быстрая навигация по каталогу
L/R — постранично; при сортировке A–Z — алфавитный индекс сбоку.
Зависит от F2.

### O12. ✅ Восстановление фокуса и скролла при возврате по B
Добавлено 2026-07-04. Из карточки игры назад в каталог по B — фокус сбрасывается,
скролл-позиция теряется. Запоминать индекс записи (грид-строка/полка + позиция
внутри полки) при уходе с экрана; при возврате (`willAppear`/onResume) после
`reloadData` скроллить `RecyclerFrame` к строке и отдавать фокус той же карточке.
Заодно перепроверить критерий F2 «фокус не теряется при reloadData» — регрессия
или незакрытый хвост; покрыть оба случая.
Зависит от F2.
**Готово, когда:** каталог → карточка игры → B возвращает фокус на ту же
карточку при любой позиции скролла, в т.ч. на полках; то же после смены
сортировки не требуется (там сброс ожидаем).
**Выполнено 2026-07-06:** каталог запоминает hash сфокусированной записи при
уходе на карточку; на `willAppear` после `reloadData` восстанавливает
скролл-позицию и фокус (грид и полки, включая hero-карточку). Критерий F2 по
`reloadData` перепроверен и покрыт: фокус ставится на ту же запись, при её
исчезновении — на соседнюю ячейку вместо сброса. PC-тесты, golden-скриншоты и
сборка Switch NRO зелёные.

---

## Sonnet — локальные правки и полировка

### S1. ✅ ETA и процент в DownloadCell
Вычислить остаток/скорость (скорость уже есть в `DownloadManager`), показать ETA и
процент числом; статус красить статусным токеном (ошибка — красная, сейчас всё cyan).
Зависит от O1 (токены), иначе делать на текущих цветах и перекрасить позже.
**Выполнено 2026-07-03:** процент показан в статусе, ETA рассчитан по оставшимся
байтам и текущей скорости; добавлены локальные статусные цвета до внедрения O1.

### S2. FilePicker: стартовый путь и вид
Стартовать в `sdmc:/switch/pipensx/torrents` (сейчас корень SD), breadcrumb в заголовке,
иконки папок/файлов, папки первыми.

### S3. ✅ Пустые состояния с CTA
Иконка + текст + кнопка («Обновить каталог» и т.п.) вместо «Press R» / «Press X»
в Catalog, Downloads, Installed.
**Выполнено 2026-07-04:** добавлен общий `EmptyStateView` на токенах O1
(`icon.png`, заголовок, пояснение, CTA-кнопка) и подключён в `CatalogView`,
`MainView`/Downloads и `InstalledView`. Catalog показывает `Refresh catalog`
или `Clear search` в зависимости от состояния поиска; Downloads — `Import
.torrent`; Installed — `Refresh installed`.

### S4. ✅ Факты таблицей на карточке игры
`DetailCell`-строки: Publisher / Release / Genre / Size / Title ID — вместо склеенной
строки. Если F3 ещё не готова — делать в текущем layout, при F3 переедет.
**Выполнено 2026-07-04** в рамках F3: строки Publisher / Release / Genre / Size /
Title ID (`addFactRow`, label/value), пустые значения скрываются.

### S5. ✅ Описание с «Show more»
Кнопка разворота вместо необратимой обрезки `shortDescription`.
**Выполнено 2026-07-04** в рамках F3: borderless-кнопка «Show more» разворачивает
полный текст (появляется только при описании длиннее 900 символов).

### S6. Тост «К загрузкам» после Install
После постановки в очередь с карточки игры — уведомление с переходом
(`tabs->focusTab(1)` уже существует для батча).

### S7. Бейдж активности на вкладке Downloads
Число активных загрузок в сайдбаре — статус виден из каталога.

### S8. Звуки фокуса и клика
Включить звуковые эффекты Borealis (форк wiliwili поддерживает); проверить
инициализацию аудио на консоли.

### S9. Ошибки карточкой с Retry
`lastFailure` / health на карточке игры — блок с иконкой и кнопкой Retry вместо
оранжевой строки.

### S10. Settings: перегруппировка
Группы «Каталог / Загрузки / Внешний вид / Диагностика»; фильтр All/Games не
дублировать с чипом каталога (O2) — оставить одно место.

### S11. ✅ About-экран
Карточка с иконкой приложения, версией, QR-кодами на Telegram/GitHub. QR — вендорить
`qrcodegen` (nayuki, MIT), рисовать NanoVG.
**Выполнено 2026-07-04:** `AboutView` перестроен в карточный экран: иконка
приложения, версия/время сборки, QR-коды на Telegram и GitHub; `qrcodegen`
вендорен в `vendor/qrcodegen`, QR рисуются через NanoVG, `icon.png` копируется
в romfs, third-party notices обновлены.

### S12. Экран applet-mode
Инструкция с картинкой (запуск через title override) вместо голого текста.

### S13. Чек-лист UI-ревью
В `CONTRIBUTING.md`: только токены (grep `nvgRGB` = 0 вне theme), обе темы,
геймпад + тач, en + ru, скриншоты приложены.

---

## Сводка зависимостей и порядок

1. **F1 → O1** — фундамент, строго первыми.
2. Дальше параллельно: **F2** (каталог), **F3** (карточка игры), **O3** (i18n), **O7/O8** (downloads).
3. На готовые экраны: O2, O4, O5, O6, O9, O10, O11.
4. Sonnet-пул — в любой момент после F1 (S2, S3, S8, S11, S12, S13 — вообще без зависимостей).
5. **F4** (golden-CI) — как только F1 готова; дальше страхует все остальные задачи.
6. Добавлено 2026-07-04 (проблемы с прогона): **F5** (полки: популярность,
   дедупликация, визуал) и **F6** (in-memory кэш обложек) — запускать через
   Fable: кросс-модульные, с производительностью/жизнью текстур и
   ранжированием; **O12** (фокус при B) — хватит Opus, задача скоупная.
   F5/F6/O12 независимы друг от друга — можно вести параллельно.

Быстрые победы для первого релиза: S1, S2, S3, S6 + O1.
